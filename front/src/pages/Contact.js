import React from 'react'

export default function Contact() {
    return (
        <div style={{backgroundColor: "gray", display:'flex', alignItems:"center", justifyContent: "center", justifyItems:"center"}}>
            <h1>Contacts page... </h1>
        </div>
      )
}